{% set name = "telnetlib3" %}
{% set version = "2.0.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/jquast/{{ name }}/archive/refs/tags/{{ version }}.tar.gz
  sha256: 3134b8fda4cff802a3f5ed39fa7c1e992d16ea62e98932096b00e7500b5b1cf8

build:
  entry_points:
    - telnetlib3-server = telnetlib3.server:main
    - telnetlib3-client = telnetlib3.client:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6

test:
  imports:
    - telnetlib3
  commands:
    - pip check
    - telnetlib3-server --help
    - telnetlib3-client --help
  requires:
    - pip

about:
  home: http://telnetlib3.rtfd.org/
  summary: Python 3 asyncio Telnet server and client Protocol library
  license: BSD-3-Clause
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - atrawog
